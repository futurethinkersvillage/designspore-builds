node_modules/
.next/
.env
.env.local
*.log
node_modules/
.next/

# Never ship per-guest data. occupancy.json contained real guest names
# and was briefly public; availability.json (dates only) is what the app uses.
public/booking/map/occupancy.json
