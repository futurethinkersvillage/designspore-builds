import DeckFrame from "@/components/DeckFrame";

// /deck2 — version 2 (nav, motion, self-hosted fonts, analytics). Also served at /deck.
export default function Deck2Page() {
  return <DeckFrame src="/deck2/index.html" />;
}
