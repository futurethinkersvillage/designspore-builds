import DeckFrame from "@/components/DeckFrame";

// /future-school4 — archived v1 of the Future School deck (kept for comparison).
export default function FutureSchool4Page() {
  return <DeckFrame src="/future-school4/index.html?v=20260716a" />;
}
