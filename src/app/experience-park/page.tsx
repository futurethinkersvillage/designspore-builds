import DeckFrame from "@/components/DeckFrame";

// /experience-park — public vision deck for the Portal.Place experience park.
// Public + chrome-free (see middleware). No investment terms — an invitation to connect.
export default function ExperienceParkPage() {
  return <DeckFrame src="/experience-park/index.html?v=20260912a" />;
}
